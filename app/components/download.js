import { DownloadCloudIcon ,FileDown } from "lucide-react";
import React from "react";

const Download = () => {
  const downloader = ()=>{

  }
  return <button
  onClick={()=>downloader()}>
    <FileDown />
  </button>
};

export default Download;
